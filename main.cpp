#define _USE_MATH_DEFINES
#include <cmath>    // M_Pi
#include <iostream> // std::cout
#include <complex>
#include "Eigen/Dense"

using complexnum = std::complex<double>; // easier to read, and if I need to change to e.g. long double through it will be easier

int hallconductivity(int Nmax, double temp, int omegamax, double deltaomega, double zeroplus);

int main()
{
    return 0;
}

int hallconductivity(int Nmax, double temp, int omegamax, double deltaomega, double zeroplus)
{
    return 0; // meaningless for now
}